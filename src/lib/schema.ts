export type Product = {
  active: boolean | null;
  description: string | null;
  price: number | null;
  promotion_price: number | null;
  quantity_in_stock: number | null;
  title: string | null;
};
